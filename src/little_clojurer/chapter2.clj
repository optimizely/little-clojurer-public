(ns little-clojurer.chapter2
  (:require [little-clojurer.chapter1 :refer :all]))

(defn lat?
  "Return true iff x is a list made of up only atoms, and not nested lists."
  [x] nil?)

(defn member?
  "Return true iff lst contains item."
  [item lst] nil?)
